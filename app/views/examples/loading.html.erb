<div class="container mx-auto p-4 max-w-5xl">
  <h1 class="text-3xl font-bold text-center mb-2">Loading States &amp; Server Events</h1>
  <p class="text-center text-base-content/70 mb-8">
    <code>live-loading</code>, <code>live-disable-with</code> and
    <code>dispatch_event</code>, on their own with adjustable latency
  </p>

  <%= live('loading_demo', id: "loading_demo") %>
</div>
