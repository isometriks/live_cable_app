<div class="container mx-auto p-4">
  <h1 class="text-3xl font-bold text-center mb-8">LiveCable Todo App</h1>
  
  <div class="grid gap-8">
    <div class="w-full flex justify-center">
      {{ live('todo_input', id: "input") }}
    </div>

    <div class="w-full flex justify-center">
      {{ live('todo_list', id: "list") }}
    </div>
  </div>
</div>
